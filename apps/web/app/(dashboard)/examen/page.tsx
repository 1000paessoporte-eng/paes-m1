export default function ModoExamenPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Modo Examen Focus</h1>
      <p className="mt-1 text-sm text-muted">
        SPA de alto rendimiento · 2h 20m · tracking en milisegundos por
        pregunta.
      </p>
      {/* TODO: shell de examen con timer, atajos de teclado y guardado
          continuo de estado (autosave a la API cada N segundos) */}
    </div>
  );
}
