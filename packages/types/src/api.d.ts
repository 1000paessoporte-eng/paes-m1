// Placeholder — se sobrescribe al correr `pnpm --filter @paes-m1/types generate`
// una vez que apps/api tenga endpoints reales expuestos en /openapi.json.
export type paths = Record<string, never>;
