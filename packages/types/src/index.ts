// `api.d.ts` se genera con `pnpm --filter @paes-m1/types generate`
// (requiere apps/api corriendo en localhost:8000). Fuente de verdad única
// de tipos compartidos entre apps/web y apps/api.
export type { paths } from "./api";
